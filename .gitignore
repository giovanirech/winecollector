# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
.eggs/

# Virtual environments
.venv/
venv/
env/

# Environment
.env
.env.*
!.env.example

# Local data (database volume + scraped images)
# Note: data/postgres/ must remain ENTIRELY empty on the host — Postgres's
# initdb refuses to initialize a mount point containing any files, including
# .gitkeep. The directory is recreated automatically on first `docker compose up`.
data/postgres/
data/images/
!data/images/.gitkeep

# Testing & coverage
.pytest_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml

# Type checking / linting cache
.mypy_cache/
.ruff_cache/

# IDEs
.vscode/
.idea/
*.swp
*~

# OS
.DS_Store
Thumbs.db
